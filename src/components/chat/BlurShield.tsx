import React, { useState } from 'react'; export default function OMNIXBlurShield({ messageContent }: { messageContent: string }) { const [isRevealed, setIsRevealed] = useState(false); return ( <div style={{ background: '#111', padding: '15px', borderRadius: '8px', border: '1px solid #ff0055', margin: '10px 0', userSelect: 'none' }}> <p style={{ fontSize: '11px', color: '#ff0055' }}>⚠️ SECURE STREAM - ANTI SCREENSHOT</p> <div onMouseDown={() => setIsRevealed(true)} onMouseUp={() => setIsRevealed(false)} onTouchStart={() => setIsRevealed(true)} onTouchEnd={() => setIsRevealed(false)} style={{ filter: isRevealed ? 'none' : 'blur(8px)', transition: 'filter 0.2s ease', background: '#000', padding: '10px', color: '#00ffcc', cursor: 'pointer', fontFamily: 'monospace' }}> {messageContent} </div> <p style={{ fontSize: '10px', color: '#555', marginTop: '5px' }}>Hold down touch to temporarily decrypt view.</p> </div> ); }
