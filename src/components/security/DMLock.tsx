import React, { useState } from 'react'; export default function OMNIXDMLock() { const [isLockEnabled, setIsLockEnabled] = useState(false); const [screen, setScreen] = useState('SETTINGS'); const [pin, setPin] = useState(''); return ( <div style={{ backgroundColor: '#04040a', color: '#fff', padding: '20px', fontFamily: 'monospace', borderRadius: '10px', border: '1px solid #00ffcc', margin: '15px' }}> <h3 style={{ color: '#00ffcc' }}>🔒 DM SECTION LOCK ENGINE</h3> {screen === 'SETTINGS' && ( <div> <p>Enable biometric or master security PIN.</p> <button onClick={() => { if (isLockEnabled) { setIsLockEnabled(false); } else { setIsLockEnabled(true); setScreen('SET_PIN'); } }} style={{ background: isLockEnabled ? '#ff0055' : 'transparent', border: '1px solid #00ffcc', color: '#fff', padding: '8px 16px', cursor: 'pointer' }}> {isLockEnabled ? 'LOCK IS ACTIVE' : 'TURN ON LOCK'} </button> </div> )} {screen === 'SET_PIN' && ( <div> <p>Create 4-Digit Secure PIN:</p> <input type='password' maxLength={4} style={{ background: '#111', border: '1px solid #ff0055', color: '#fff', textAlign: 'center', width: '80px', padding: '5px' }} onChange={(e) => setPin(e.target.value)} /> <button onClick={() => setScreen('SETTINGS')} style={{ marginLeft: '10px', background: '#00ffcc', color: '#000', border: 'none', padding: '5px 10px', fontWeight: 'bold' }}>SAVE</button> </div> )} </div> ); }
